<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <androidx.viewpager.widget.ViewPager
        android:layout_width="wrap_content"
        android:layout_above="@id/mBottomNavigationView"
        android:layout_height="match_parent"
        android:id="@+id/mViewPager"/>
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/mBottomNavigationView"
        app:itemIconTint="@color/color_onclick_item"
        app:itemTextColor="@color/color_onclick_item"
        app:menu="@menu/menu_spending"
        android:background="@drawable/background_nen"
        android:layout_alignParentBottom="true"/>
</RelativeLayout>